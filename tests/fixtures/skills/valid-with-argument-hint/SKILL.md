---
name: valid-with-argument-hint
description: A valid fixture that uses the optional `argument-hint` field to document its invocation surface.
argument-hint: "[--posture auto|draft|comment-only] [optional positional]"
requires-skills: []
requires-config: []
---

# Valid With Argument Hint

Fixture for `validate.sh` — demonstrates the optional `argument-hint` field is accepted.
