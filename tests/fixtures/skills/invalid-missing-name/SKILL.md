---
description: This fixture deliberately omits the required `name` field so the validator must reject it.
requires-skills: []
requires-config: []
---

# Invalid: missing name

`validate.sh` must reject this file because `name` is a required frontmatter field.
